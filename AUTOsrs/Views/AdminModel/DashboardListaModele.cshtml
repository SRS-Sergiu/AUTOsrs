@model IEnumerable<AUTOsrs.Models.ModelAutoModel>


@{
    ViewBag.Title = "Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista Marci</h2>

<p>
    @Html.ActionLink("Create New", "DashboardCreareModel")
</p>
<table class="table asd">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Marca)
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marca)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditModel", new { id = item.ID_Model }) |
                @Html.ActionLink("Delete", "DeleteModel", new { id = item.ID_Model })
            </td>
        </tr>
    }

</table>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}